<div class="p-2">
    <div class="overflow-x-auto">
        <table class="table">
            <!-- head -->
            <thead>
                <tr>
                    <th>Ime</th>
                    <th>Broj bodova</th>
                </tr>
            </thead>
            <!-- body -->
            <tbody>
                    {{#each result.items}}
                <tr>
                    <td>{{this.korisnik}}</td>
                    <td>{{this.bodovi}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
</div>